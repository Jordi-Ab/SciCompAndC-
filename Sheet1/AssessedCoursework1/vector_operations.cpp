/*
This cpp scripts holds all the functions to perform vector operations.
*/

#include <iostream>

/*Function Prototypes declaration.*/

/*
Usage: a_vec = createVec(3);
----------------------------
Receives: an integer that is the size of the vector.
Returns: a pointer to an array object of the given size.
The returned array is empty.
*/
int* newVec(size);


int* newVec(size){
	int* vec;
	vec = new int[dim];
	return vec;
}

void setAt(int* vec, )
